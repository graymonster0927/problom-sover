# AI2AI 任务列表

## 当前进度

### 阶段一：全局文本选择监听 ✅ 代码完成（等待构建验证）

**完成内容：**
- 项目初始化：`text-ai-plugin/` Tauri 2.x 项目结构已按技术约束完整搭建
- `platform/mod.rs`：定义 `PlatformSelector` trait + `SelectionEvent { text, x, y }` + 平台工厂函数 + 后台监听启动
- `platform/macos.rs`：macOS 基于 osascript 轮询（生产可替换为 AXObserver）
- `platform/windows.rs`：Windows SetWinEventHook + WinEvent callback
- `platform/linux.rs`：Linux xclip/xsel PRIMARY selection 轮询
- 单元测试：每个平台文件均含 `#[cfg(test)]` 测试

**关键文件：**
- `src-tauri/src/platform/` — 三平台适配
- `src-tauri/src/models/` — SelectionEvent, AgentChunk, AppSettings, HistoryRecord
- `src-tauri/src/errors/` — AppError 统一错误
- `src-tauri/src/state/` — AppState (settings + cancel_token)

### 阶段二：悬浮球窗口 ✅ 代码完成

**完成内容：**
- `tauri.conf.json`：float_ball 窗口（无边框/透明/置顶/56x56）
- `commands/window_command.rs`：`show_float_ball(x,y)` / `hide_float_ball()`
- `src/components/FloatBall.tsx`：渐变玻璃圆形悬浮球，含 loading spinner
- `src/pages/FloatBallPage.tsx`：独立窗口页，监听 selection_event
- `src/hooks/useSelectionEvent.ts`：监听后端事件，触发显示悬浮球

### 阶段三：Agentic AI 核心 ✅ 代码完成

**完成内容：**
- `services/agent_service.rs`：ReAct 循环 + 多 Provider（OpenAI/Anthropic/Ollama）+ SSE 流式推送
- `Tool` trait + `WebSearchTool`(DuckDuckGo) + `SummarizeTool`
- `commands/app_command.rs`：`solve_with_ai` / `stop_ai`，CancellationToken 支持
- `repositories/history_repo.rs`：JSON 持久化，分页，with_path 测试构造器
- `tests/agent_integration_test.rs`：mockito mock HTTP 服务器测试

### 阶段四：结果展示面板 ✅ 代码完成

**完成内容：**
- `tauri.conf.json`：result_panel 窗口（420x520/透明/置顶）
- `src/components/ResultPanel.tsx`：react-markdown + rehype-highlight
- `src/pages/ResultPage.tsx`：流式展示，复制/重新生成/关闭
- `src/pages/HistoryPage.tsx`：历史列表，展开详情，分页加载，清空

### 阶段五：设置与配置管理 ✅ 代码完成

**完成内容：**
- `services/settings_service.rs` + `repositories/settings_repo.rs`
- `commands/system_command.rs`：get/save_settings, list/clear_history
- `src/pages/SettingsPage.tsx`：Provider 切换、API Key 输入、模型选择、插件开关
- `lib.rs`：系统托盘（Enable/Settings/History/Quit）
- `src/hooks/useSettings.ts`：设置读写 hook
- `tests/settings_integration_test.rs`：集成测试

### 阶段六：打包与 CI/CD ✅ 代码完成

**完成内容：**
- `.github/workflows/build.yml`：三平台矩阵构建 + 测试 job

---

## 当前状态

**后台任务（虚机）：**
1. 系统依赖（libwebkit2gtk 等）安装完成
2. tauri-cli v2.10.1 正在编译安装中
3. npm install 正在执行中

**下一步：**
- 等待 tauri-cli 安装完成
- 执行 `cargo check` 验证 Rust 代码编译
- 执行 `cargo test` 跑单元/集成测试
- 根据编译错误修复代码

## 架构总览

```
text-ai-plugin/
├── src/                    # React 前端
│   ├── components/         # FloatBall, ResultPanel, HistoryItem
│   ├── pages/              # FloatBallPage, ResultPage, HistoryPage, SettingsPage
│   ├── hooks/              # useSelectionEvent, useAiStream, useSettings
│   ├── services/           # aiService, windowService, settingsService
│   ├── store/              # Zustand appStore
│   └── main.tsx            # 按 window label 路由
├── src-tauri/
│   └── src/
│       ├── platform/       # macOS / Windows / Linux 文本选择监听
│       ├── commands/       # Tauri commands
│       ├── services/       # agent_service (ReAct+SSE), settings_service
│       ├── repositories/   # history_repo, settings_repo (JSON持久化)
│       ├── models/         # SelectionEvent, AppSettings, HistoryRecord...
│       ├── state/          # AppState
│       ├── utils/          # http, sse, logger
│       └── errors/         # AppError
└── tests/                  # 集成测试
```
